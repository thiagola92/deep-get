name: Upload Python Package

on:
  release:
    types: [published]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: thiagola92/la-deep-get/.github/workflows/python-package.yml@main
      - run: pdm publish --username __token__ --password ${{ secrets.PYPI_TOKEN }}
